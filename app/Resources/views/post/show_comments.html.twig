    {% for comment in comments %}
        <div class="card bg-light m-3">
            <h4 class="mt-3 mr-3 ml-3">{{ comment.title }}</h4>
            <p class="mr-3 ml-3">{{ comment.content }}</p>
        </div>
    {% endfor %}
